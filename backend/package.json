{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",                       // Comando para desarrollo (si estás usando Vite para los assets)
    "build": "vite build",               // Comando para construir los assets (si usas Vite)
    "start": "php -S 0.0.0.0:3000 -t public"  // Comando para iniciar Laravel usando el servidor PHP integrado
  },
  "devDependencies": {
    "axios": "^1.7.4",
    "laravel-vite-plugin": "^1.0",
    "vite": "^5.0"
  }
}
